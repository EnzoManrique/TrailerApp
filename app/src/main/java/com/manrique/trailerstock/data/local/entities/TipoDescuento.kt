package com.manrique.trailerstock.data.local.entities

/**
 * Enum que define el tipo de descuento de una promoción.
 */
enum class TipoDescuento {
    PORCENTAJE,  // Descuento en porcentaje (ej: 20%)
    MONTO_FIJO   // Descuento en monto fijo (ej: $500)
}
